import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  mapPreview: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  mapImage: {
    // add percentage to show image
    width: '100%',
    height: '100%',
  },
});
